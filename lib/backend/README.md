# Backend Layer

Estrutura recomendada para alinhar frontend Flutter com o contrato HTTP:

- `core`: tipos base, enums e contrato de rota
- `models`: DTOs e entidades usadas pelo app
- `schemas`: nomes de campos e schemas esperados pela API
- `api`: catalogo de endpoints por dominio
